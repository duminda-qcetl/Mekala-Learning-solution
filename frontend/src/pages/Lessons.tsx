import React from 'react';
import { Container, Typography } from '@mui/material';

const Lessons: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Lessons Page</Typography>
    </Container>
  );
};

export default Lessons;
