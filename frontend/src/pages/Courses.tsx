import React from 'react';
import { Container, Typography } from '@mui/material';

const Courses: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Courses Page</Typography>
    </Container>
  );
};

export default Courses;
