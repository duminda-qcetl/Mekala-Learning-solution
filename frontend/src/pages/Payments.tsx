import React from 'react';
import { Container, Typography } from '@mui/material';

const Payments: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Payments Page</Typography>
    </Container>
  );
};

export default Payments;
