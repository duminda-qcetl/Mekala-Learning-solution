import React from 'react';
import { Container, Typography } from '@mui/material';

const Practice: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">AI Practice Page</Typography>
    </Container>
  );
};

export default Practice;
